<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.serendipity.model.ReplyDAO">
	
	<select id="replyList" resultType="kr.co.serendipity.model.ReplyDTO">
		select reply_num,user_num, reply_content, reply_date from reply where review_num=#{review_num}
	</select>
	
	<insert id="replyWrite">
		insert into reply(reply_num, user_num, review_num, reply_content, reply_date) 
		values(reply_seq.nextval, #{user_num}, #{review_num}, #{reply_content}, sysdate)
	</insert>
	
	<delete id="replyDelete">
		delete from reply where reply_num=#{reply_num}
	</delete>
</mapper>